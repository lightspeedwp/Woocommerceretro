/**
 * Blog Blocks - WordPress Block Library Components
 * 
 * Optimized for Figma Make parser:
 * 1. No modern export-from syntax
 * 2. Named imports/exports
 */

import * as CategoryFilterModule from './CategoryFilter';

export var CategoryFilter = CategoryFilterModule.CategoryFilter;
